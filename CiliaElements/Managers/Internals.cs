namespace CiliaElements
{
    public static partial class TManager
    {
        #region Private Methods

        private static void MoverSpider()
        {
            //Mtx4 rp = Mtx4.CreateRotationZ(+.2);
            //Mtx4 rm = Mtx4.CreateRotationZ(-.2);
            //Mtx4 m;
            //Random rnd = new Random();
            //while (true)
            //{
            //    m = rp; rp = rm; rm = m;
            //    Spider.Links.Values[1].Matrix = rp;
            //    Spider.Links.Values[0].Matrix = rm;
            //    m = Mtx4.CreateRotationZ(0.004 * (rnd.NextDouble() - 0.5));
            //    Spider.Matrix = m * Spider.Matrix;
            //    m = Mtx4.CreateTranslation(0.001 * (Vec3)m.Column1);
            //    Spider.Matrix = m * Spider.Matrix;
            //    //foreach (TLink l in Spiders)
            //    //{
            //    //    l.Links.Values[1].Matrix = rp;
            //    //    l.Links.Values[0].Matrix = rm;
            //    //    m = Mtx4.CreateRotationZ(rnd.NextDouble() - 0.5);
            //    //    l.Matrix = m * l.Matrix;
            //    //    m = Mtx4.CreateTranslation(0.1 * (Vec3)m.Column1);
            //    //    l.Matrix = m * l.Matrix;
            //    //}
            //    Thread.Sleep(1);
            //}
        }

        #endregion Private Methods
    }
}